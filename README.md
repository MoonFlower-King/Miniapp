# 🎋 知岁 (ZhiSui) - PocketLedger

**知岁**，知晓岁月，记录点滴。
这是一款集 **极简记账**、**高效清单** 与 **AI 智能助手** 于一体的个人生活管理 App。

---

## ✨ 核心亮点

### 1. 💰 知岁账本 (Smart Ledger)

拒绝繁琐，回归记录本质。

* **双向记录**: 快速记录收入与支出。
* **可视化图表**: 首页动态圆环图 (Ring Chart)，财务状况一目了然。
* **分类统计**: 支持餐饮、交通、购物等 10+ 种常用分类。

### 2. ✅ 知岁清单 (Pro Tasks)

不仅是待办，更是您的个人项目管理工具 (Notion-style)。

* **全能属性**: 支持 **状态**、**优先级**、**截止日期**、**负责人** 及 **附件**。
* **灵活视图**: 只有在您需要时，才展示复杂的属性字段（支持自定义可见性）。
* **高效交互**: 点击状态图标直接流转，长按管理，拖拽排序（规划中）。

### 3. 🤖 AI 智能助手 (Powered by DeepSeek)

说人话，办实事。

* **语音记账**: “刚才打车花了30块” -> 自动生成账单。
* **智能解析**: “下周五交实验报告” -> 自动创建带截止日期的任务。
* **贴心问答**: 生活百科、财务建议，有问必答。

---

## 🛠 技术架构

本项目作为 Android 个人开发作业的典范，采用标准的 **MVC** 架构与 **现代化 Android 组件**。

* **开发语言**: Java
* **界面框架**: Fragment + ViewPager2 + RecyclerView
* **数据存储**: SQLite (DatabaseHelper) + SharedPreferences
* **网络通信**: OkHttp + Glide (图片加载)
* **AI 模型**: 集成 DeepSeek V3 API

### 目录结构

```
com.example.pocketledger
├── MainActivity.java       // 启动页与逻辑分发
├── MainContainerActivity.java // 主容器 (ViewPager2)
├── LedgerFragment.java     // 记账模块
├── TaskFragment.java       // 任务模块
├── AiFragment.java         // AI 对话模块
├── model
│   ├── Transaction.java    // 账单实体
│   └── TodoItem.java       // 任务实体 (v2)
├── adapter                 // 列表适配器
└── utils
    ├── DatabaseHelper.java // SQLite 数据库管理
    └── AiManager.java      // AI 网络请求封装
```

---

## 📥 安装与运行

1. **环境要求**: Android Studio Koala 或更高版本，JDK 17+。
2. **构建**:
   ```bash
   ./gradlew assembleDebug
   ```
3. **安装**: 生成的 APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。

---

## 📝 版本历史

* **v1.0.0** (2025-12)
  * 更名为“知岁”。
  * 新增 Notion 风格任务属性（负责人、附件）。
  * 新增 AI 智能记账功能。
  * 优化 UI，升级至 Material Design 风格。

---

**开发者**: 江月
